apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
# stringData を使うと base64 エンコード不要
# 本番では kubectl create secret コマンドで作成し、このファイルは Git に含めないこと
stringData:
  MYSQL_ROOT_PASSWORD: "rootpass"
  MYSQL_DATABASE: "myapp"
  MYSQL_USER: "appuser"
  MYSQL_PASSWORD: "apppass"
